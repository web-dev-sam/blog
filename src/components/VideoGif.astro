---
const defaultStyle = "margin: 3rem auto;";
---
<video
  autoplay
  muted
  loop
  style={Astro.props.style || defaultStyle}
  aria-label={Astro.props.label}
  title={Astro.props.title}
>
  <source src={Astro.props.src} type="video/webm" />
</video>